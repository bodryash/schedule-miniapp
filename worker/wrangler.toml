name = "fgp-schedule-bot"
main = "src/index.js"
compatibility_date = "2026-09-03"

# Разбор очереди рассылки: за один запуск воркер успевает отправить порцию,
# остальное уходит на следующей минуте.
[triggers]
crons = ["* * * * *"]

# Токен и секрет вебхука не хранятся в файлах — они задаются командой
# `npx wrangler secret put ...` и лежат на стороне Cloudflare.

# Кто писал боту. Telegram не отдаёт список пользователей, поэтому без
# собственной записи рассылку сделать не по чему.
# Статистика открытий и список пользователей бота. Список раньше лежал в KV,
# но там всего тысяча записей в сутки — в день массовой раздачи ссылки лишние
# молча не попали бы в рассылку.
[[d1_databases]]
binding = "STATS"
database_name = "schedule-stats"
database_id = "2441ab71-d521-4771-8dbc-4d34771dd732"
