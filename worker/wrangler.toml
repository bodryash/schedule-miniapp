name = "fgp-schedule-bot"
main = "src/index.js"
compatibility_date = "2026-09-03"

# Токен и секрет вебхука не хранятся в файлах — они задаются командой
# `npx wrangler secret put ...` и лежат на стороне Cloudflare.
