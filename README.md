# Расписание ФГП МГУ — Telegram Mini App

Мини-приложение показывает расписание занятий: студент один раз выбирает
группу, дальше сразу видит свой день.

Открыто по адресу <https://bodryash.github.io/schedule-miniapp/>.

## Из чего состоит

| Что | Где |
| --- | --- |
| Страница мини-приложения | `docs/` — статика, без сборки |
| Данные расписания | `docs/data/schedule.json` |
| Парсер PDF | `tools/parse_pdf.py` |
| Бот на вебхуке (боевой) | `worker/` — Cloudflare Worker |
| Бот на опросе (локальный) | `bot.py` |

## Сверить с исходником

```bash
python tools/check.py "путь/к/расписанию.pdf"
```

Проверяет привязку пар к группам по прямоугольникам ячеек: их границы заданы
линиями таблицы, поэтому какие колонки накрывает объединённая ячейка, видно
из координат, а не из эвристики. День определяется в проверяльщике заново,
независимо от парсера. Запускать после каждого нового PDF.

## Обновить расписание

```bash
python tools/parse_pdf.py "путь/к/расписанию.pdf"
```

Перезаписывает `docs/data/schedule.json`. После этого закоммитить и запушить —
GitHub Pages раздаёт папку `docs/` с ветки `main`.

## Запустить локально

```bash
python -m http.server 8123 --directory docs
```

## Бот

Боевой бот живёт в Cloudflare Worker и работает без включённого
компьютера: Telegram сам шлёт обновления на вебхук.

- Бот: [@FGPshedulebot](https://t.me/FGPshedulebot)
- Воркер: <https://fgp-schedule-bot.bodryash.workers.dev>

Разворачивается из папки `worker/`:

```bash
npx wrangler deploy
```

Токен и секрет вебхука в файлах не лежат — они задаются один раз:

```bash
npx wrangler secret put BOT_TOKEN
```

```bash
npx wrangler secret put WEBHOOK_SECRET
```

Секрет вебхука Telegram присылает в заголовке `X-Telegram-Bot-Api-Secret-Token`.
Без этой проверки на публичный адрес воркера мог бы слать поддельные
обновления кто угодно.

### Статистика открытий

Кнопка меню открывает мини-приложение напрямую, минуя бота, поэтому про эти
открытия бот сам ничего не знает — сообщает о них само приложение.

Запрос уходит на `POST /hit` вместе с `initData`, подписанной Telegram
токеном бота. Воркер проверяет подпись: без этого счётчик накрутил бы кто
угодно одной командой. Схема — в [worker/schema.sql](worker/schema.sql).

Кто заходил, видно поимённо — так решил владелец. Истории просмотров всё
равно нет: только «этот человек заходил тогда-то с такой группой». Одно
открытие засчитывается не чаще раза в час, иначе один человек за пару
перевесил бы целую группу.

Кто **не** заходил, отсюда узнать нельзя: про человека, ни разу не
открывшего приложение, в базе нет вообще ничего. Ближайший доступный ответ —
список групп без единого открытия.

Команды, обе только владельцу (`OWNER_ID`):

- `/stats` — сводка: открытия за день и неделю, активные группы, языки
  Telegram за неделю, молчащие группы.

Языки хранятся в таблице `langs` только суммой по дню (`2026-09-10 · zh · 5`),
без привязки к людям. Нужны, чтобы понять, сколько иностранцев и стоит ли
переводить приложение — например, на китайский.
- `/who` — кто заходил и когда. `/who 311гэу` — только по этой группе.

### Рассылка

Telegram не отдаёт список пользователей бота — его можно только вести самому.
Воркер записывает каждый чат, который написал боту, в таблицу `users`; без
этой записи рассылать было бы некому.

Раньше список лежал в KV, но там всего тысяча записей в сутки. В день, когда
ссылку раздают всему потоку, лишние молча не попали бы в список — и не
получили бы будущую рассылку. В базе лимит стократно выше.

Обычный путь — прямо из Telegram, с телефона:

```
/broadcast Завтра занятий нет
```

Бот покажет текст и число получателей с кнопками «Отправить» и «Отмена».
Отозвать рассылку нельзя, поэтому подтверждение обязательно.

Отправка идёт не сразу: воркеру нельзя делать больше полусотни обращений
наружу за один запуск, а получателей сотни. Поэтому письма складываются в
очередь `outbox`, а задача по расписанию разбирает её порциями по сорок штук
раз в минуту. По окончании бот присылает итог: сколько дошло, сколько нет.

Запасной путь с компьютера, если нужен длинный текст из файла:

```bash
python tools/broadcast.py message.txt
```

Без `--send` это черновой прогон: скрипт покажет текст и число адресатов, но
ничего не отправит.

Достучаться можно только до тех, кто когда-либо писал боту: Telegram не
позволяет писать первым. Открывшие приложение по прямой ссылке, но не
нажимавшие `/start`, в рассылку не попадут.

### Расписание в чатах

В любой переписке пишешь `@FGPshedulebot 311гэу` — над клавиатурой
появляются «Сегодня», «Завтра» и «Вся неделя», нажатие отправляет карточку с
парами и кнопкой «Открыть расписание». Понимает `311гэу завтра`, `311 чт`,
`311гэу неделя`, `251 гэу ин`; по началу названия (`31`) подсказывает группы.
Пустой запрос показывает группу, из которой человек открывал приложение.

Код — `worker/src/inline.js`. Данные — файлы по группам
`docs/data/groups/<группа>.json`, их пишет `parse_pdf.py` вместе с общим
расписанием: целый `schedule.json` весит мегабайт, а отвечать надо быстро.
Какие языки у человека, в чате неизвестно (выбор хранится на телефоне), поэтому
языки одной пары сведены в строку «по подгруппам», а одинаковые пары подряд —
в «1–6». Время московское. В воскресенье предлагается понедельник.

Включение, один раз:

1. BotFather → `/setinline` → выбрать бота → подсказка `группа и день`.
2. `python tools/set_webhook.py` — вебхук начнёт получать `inline_query`.

### Объявления об изменениях

```
/notice 311гэу Пара в четверг переносится в 614
/notice 311гэу,312гэу Текст
/notice 3курс Текст    — курсу бакалавриата (1курс … 4курс)
/notice маг1 Текст     — курсу магистратуры (маг1, маг2)
/notice все Текст
/notice            — список действующих
/unnotice 12       — снять
/unnotice все      — снять все действующие разом
```

Только от владельца. Текст показывается жёлтой плашкой вверху расписания у
этих групп (крестик скрывает её на телефоне) и первой строкой в карточках
расписания в чатах. Через неделю объявление снимается само. Хранится в
таблице `notices`; приложение забирает их через `GET /notices?group=…` у
воркера — сайт статический и базу не видит.

Перед первым использованием таблицу надо создать:

```bash
cd worker && npx wrangler d1 execute schedule-stats --remote --file schema.sql
```

Объявление на курс хранится одной записью с ключом `курс:<ступень>:<номер>`,
а не копией на каждую группу: снимается одной командой, и группа, появившаяся
в новом PDF, его тоже увидит. Приложение для этого передаёт воркеру курс и
ступень вместе с группой.

### Отмена пар

```
/cancel 311гэу 14.09 3 Преподаватель заболел
/cancel 3курс завтра          — весь день
/cancel 311гэу,312гэу пт 1-2
/cancel все 15.09 5,6
/cancel                       — список предстоящих отмен
/uncancel 7                   — вернуть пару
```

Порядок: кому (как в `/notice`), день (`14.09`, `сегодня`, `завтра`, `пн`…`сб`
— ближайший), номера пар (`3`, `1-2`, `5,6`; без них — весь день), причина.
Только от владельца.

В приложении карточка остаётся на месте, но гаснет, название зачёркнуто,
красным — «Отменена · причина». «Идёт сейчас» и «дальше» такие пары
пропускают; если отменено всё, строка «дальше» так и пишет. В расписании в
чатах пара зачёркнута с пометкой «отменена». Отмены хранятся в таблице
`cancels` и приходят приложению тем же запросом, что объявления.

Свободные аудитории отмены пока не учитывают: аудитория отменённой пары
по-прежнему считается занятой.

### Локальный бот

Локальный `bot.py` остаётся для отладки, но **одновременно с вебхуком он не
работает**: Telegram отдаёт обновления либо туда, либо сюда. Чтобы вернуться к
опросу, вебхук надо удалить (`deleteWebhook`), и наоборот. Для `bot.py` нужен
`.env` с `BOT_TOKEN`.

## Формат данных

`schedule.json`:

- `meta` — семестр, факультет, даты начала и конца.
- `weeks` — календарь чётных и нечётных недель из примечания к расписанию.
  Чётность берётся отсюда, а не считается по формуле: первая неделя семестра
  укорочена, и арифметика по номеру недели даёт неверный ответ.
- `bells` — время пар.
- `groups` — `id`, `title`, `course`, `level` (`бакалавриат` / `магистратура`).
  Курс без уровня неоднозначен: первый курс есть и там, и там.
- `lessons` — по одной записи на пару и группу:
  `group`, `day` (1 = понедельник), `slot`, `week` (`all` / `odd` / `even`),
  `subject`, `type`, `teacher`, `room`, `subgroup`, `elective`
  (`null`, `«по выбору»`, `«факультатив»`), `note`, `link` — ссылка на
  встречу для занятий на удалёнке, `start` и `end` — своё время, если пара
  идёт не по сетке звонков.

Своё время в исходнике приписано прямо к названию: «12.00-13.30 2-ой
Немецкий язык». Пока оно оставалось в названии, предмет не совпадал с
обычным «2-ой Немецкий язык» и проскакивал мимо выбора языка, а время
показывалось из сетки звонков — то есть неверное.

## Обновления доходят до студентов

`app.js` и `style.css` подключаются с меткой времени. Без неё Telegram
показывает закэшированную версию неизвестно сколько, и исправление
доезжает не до всех: пользователь видит старое поведение и справедливо
считает, что баг не починен.

Сам `index.html` GitHub Pages отдаёт с `max-age=600`, поэтому новая версия
подхватывается в течение десяти минут.

## Настройки студента

При первом запуске приложение спрашивает курс, группу, основной язык с
подгруппой и второй (или третий) язык с подгруппой.

Основной язык — это либо английский, либо русский для иностранцев; вместе они
не встречаются, поэтому спрашиваются одним вопросом. Названия зависят от
курса: на младших «Английский язык» и «Русский язык как иностранный», на
старших «Профессиональный английский» и «Профессиональный русский».

Списки строятся по самим данным: для 1 курса второго языка нет, и поле не
показывается; если подгруппа всего одна, вопрос тоже пропускается. Ответы
лежат в `localStorage` под ключом `schedule.prefs`.

Последним блоком идут галочки дисциплин по выбору — список строится по парам
самой группы. Отмеченные останутся в расписании, остальные скроются. Если не
отмечено ничего, показываются все: иначе студент, пропустивший этот блок,
получил бы расписание без половины занятий.

Дальше языковые пары чужих подгрупп скрыты.

## Поиск

Кнопка с лупой открывает поиск по преподавателю, аудитории, предмету и номеру
группы разом — по всему расписанию, а не только по своей группе.

Одну лекцию читают сразу нескольким группам, поэтому одинаковые занятия
сводятся в одну строку со списком групп: иначе «Шестова» выдавала бы двадцать
пять почти одинаковых карточек вместо десяти.

## Свободные аудитории

Кнопка с дверью в шапке показывает, какие аудитории свободны в выбранный
день на выбранной паре и до какого времени — чтобы найти, где посидеть между
парами. По умолчанию открывается идущая или ближайшая пара.

Учитываются только аудитории своего корпуса — номера вида `614`, `611Б`.
Аудитории других факультетов (`638Б ЮФ`, `558 ВШССН`) исключены: по
расписанию ФГП они «свободны» почти всегда, а на деле заняты своими.
Военная кафедра, спортбаза, дистант и виртуальные — тоже не места.

Занятость считается по пересечению времени, а не по номеру пары: у части
занятий своё время, и «12:00–13:30» задевает сразу две соседние пары.
Мероприятия вне расписания, разумеется, не видны.

## Что дальше

Под днями стоит строка с ближайшей парой и временем до неё. Показывается
только для сегодняшнего дня: в чужом дне «дальше» не существует. Когда пары
кончились — так и написано.

Пара считается идущей, только если она есть в расписании этой группы: звонок
звенит для всех, но у половины групп в это время ничего нет.

## Цвета занятий

- Зелёный — дисциплина по выбору.
- Фиолетовый — межфакультетский курс МГУ.
- Приглушённый, без заливки — факультатив. Военная подготовка занимает шесть
  пар подряд, и в обычном виде она перебивала обязательные занятия.

Эти цвета говорят про тип занятия, поэтому не зависят от чётности недели и не
меняются вместе с акцентом.

## Окна

Свободная пара между занятыми показывается отдельной строкой «ОКНО» с номером
пары и временем. Пустые пары в начале и в конце дня окнами не считаются.

## Календарь

Сверху стоит дата выбранного дня. Полоса дней тянется на две недели —
текущую и следующую — и листается вбок; недели разделены чертой, сегодняшнее
число подсвечено акцентом, даже когда пролистали на другой день. Дальше двух
недель не идём: расписание на послезавтра нужно часто, на месяц вперёд — нет.

Приложение открывается на сегодняшнем дне. В воскресенье занятий нет, поэтому
показывается понедельник уже следующей недели, а не той, что закончилась, и
полоса начинается с него.

Чётность считается по неделе целиком, а не по конкретному дню: иначе
понедельник 31.08 выпал бы из семестра, начатого 02.09, и подпись недели
менялась бы при переключении дней.

## Текущая пара

Идущая пара определяется по часам телефона и подсвечивается акцентом: точка,
подпись «идёт сейчас», сколько осталось до конца и полоса прогресса по низу
карточки. При открытии приложение подводит к ней, если она не попала на экран.

Полоса — WAAPI-анимация длиной во всю пару: идёт на компоновщике, как
CSS-анимация, но её позицию можно выставить по часам. Это важно: таймлайн
анимаций останавливается, пока приложение свёрнуто, и CSS-вариант после
возвращения показывал бы момент, на котором его прервали. Поэтому позиция
сверяется с часами на каждом обновлении и при возврате в приложение
(`visibilitychange`), а между сверками анимация идёт сама.

Текст «осталось N мин» обновляется там же, раз в 30 секунд, поверх готовых
карточек: перерисовка списка заново проигрывала бы появление каждые полминуты.

Всё время берётся с устройства — часовой пояс и часы телефона. Звонки в PDF
московские, так что телефон в другом поясе покажет «идёт сейчас» не ту пару.
Если приложение оставили открытым до следующего дня, страница перезагружается —
точечного обновления там уже мало.

## Движение

- Смена дня — появление карточек за 180 мс с каскадом по 25 мс, ограниченным
  120 мс: список в длинный день не должен ждать свою анимацию дольше, чем
  читается. Пришли листанием — карточки въезжают с той стороны, откуда тянули.
- Листание дней — расписание ведётся за пальцем. День меняется, если
  протянули больше 22% ширины экрана **или** сделали быстрый флик; иначе
  возвращается на место. На краях ленты сопротивление растёт, но упора нет.
  Направление жеста определяется по первым 8 пикселям: если движение скорее
  вертикальное, жест отдаётся прокрутке (`touch-action: pan-y`).

  Касание у левого или правого края экрана (22% ширины) листает на день, как
  в сторис. Отличается от жеста по тому, сдвинулся ли палец больше чем на
  8 пикселей. Раскрытие подгрупп, ссылки и поля из этого исключены — причём
  по цели нажатия, а не отпускания: палец мог сместиться.

  Слушается документ, а не блок расписания: у `body` есть отступ, и крайние
  пиксели экрана блоку не принадлежат — а именно туда и приходится касание у
  края. Заодно это решает случай пустого дня, где тянуть не за что: в субботу
  у первого курса пар нет вовсе. Полоса дней и кнопки исключены — им
  горизонталь нужна самим.
- Переход между расписанием и настройками — 200 мс.
- Нажатие — сжатие до 0.97 за 120 мс.

Всё держится на `transform` и `opacity`. При `prefers-reduced-motion` движение
убирается, а смысловые появления остаются как проявление прозрачности.

## Цвет недели

Нечётная неделя — голубой интерфейс, чётная — оранжевый. Переключается
атрибутом `data-parity` на `<body>`; вне семестра берётся акцент темы
Telegram.

## Известные ограничения

- Аудитория пустая там, где её нет в самом PDF.
- Узкие колонки обрезают длинные названия, а хвост переливается в соседнюю
  клетку. `repair_truncated()` достраивает такие пары по полной версии из
  широкой колонки и выбрасывает переливы; при смене вёрстки это первое
  место, куда стоит посмотреть.
- Третий язык спрашивается отдельно от второго: его берут вдобавок, а не
  вместо. По умолчанию «не хожу» — ходят на него единицы.
- «Военная подготовка» занимает подряд несколько пар, из-за чего окон в эти
  дни не остаётся.
- Разбор рассчитан на вёрстку расписания ФГП. Другой факультет потребует
  правок в `tools/parse_pdf.py`.
